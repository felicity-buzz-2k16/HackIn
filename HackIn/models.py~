from django.db import models
from django.contrib.auth.models import User

# Create your models here.

class previousLevel(models.Model):
    userid = models.ForeignKey(User)
    previous_level = models.IntegerField(default=0)
